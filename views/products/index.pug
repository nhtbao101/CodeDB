extend ../layout/common.pug
block content
    h1 Product list            
    form.ml-auto(action="/product/search" method="GET" class="d-flex") 
            input(type="text" name="key" class="form-control")
            button(class="btn btn-primary ml-2" ) Search 

    each product in products
        table
            tr(class="mr-3 pd-3")
                td
                    div(class="d-block")
                        span= product.name
                        span= product.price
                        img(src=product.image)
                        button(class="btn btn-primary mt-3") Add to Card
                td
                    
                td
                   
                td
                   
                    //- span= product.image

                //- div(class="d-grid p-2 bg-primary text-white")
                //-     span= product.name
                //-     span= product.price
                //-     span= product.image
                //- td
                    
                    //- a.btn.btn-light(href='/products/'+ product.id) View